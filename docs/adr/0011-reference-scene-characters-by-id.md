# Reference Scene Characters by ID

Scene outlines reference POV and participating characters by stable character IDs, not names. Character names are display text only: UI and prompt context may render names, but must resolve them from IDs. Missing referenced character IDs fail closed, and old name-based scene fields are removed because existing novel files will be discarded.
