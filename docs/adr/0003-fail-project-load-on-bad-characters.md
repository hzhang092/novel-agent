# Fail project load on bad required files

Status: accepted

Required project lists must not silently omit broken entries. Normal app paths should fail project load with a visible error listing bad character or outline files, while a separate repair or doctor flow may collect all errors without enabling generation from a partial cast or incomplete story structure.
